# <img src="feather.png" width="32" align="center"> MyFicbook

Мощный инструмент для автоматического скачивания, организации и
синхронизации вашей библиотеки фанфиков с сайта
[Ficbook.net](https://ficbook.net).

![Python](https://img.shields.io/badge/Python-3.8%2B-blue)
![Selenium](https://img.shields.io/badge/Selenium-Undetected-green)
![License](https://img.shields.io/badge/License-MIT-orange)

## ✨ Возможности

-   **📥 Умная синхронизация:** Скачивает только новые главы и фанфики. Если работа уже есть на диске --- пропускает.
-   **🚫 Обход лимитов:** Использует метод "чтения", а не скачивания файлов, что позволяет сохранять **тысячи работ в день** без блокировок (лимит 10 файлов/сутки не действует).
-   **📂 Структура:** Автоматически раскладывает работы по папкам, соответствующим вашим Сборникам.
-   **📑 Оглавления:** В каждой папке создает файл `– (ОГЛАВЛЕНИЕ) –.txt` с группировкой работ по **Фэндомам**, с указанием Пейрингов и Авторов.
-   **📱 Формат для чтения:** Сохраняет в `.txt` (UTF-8 with BOM), который корректно открывается на любых телефонах и читалках.
-   **🛡️ Защита от банов:** Умные алгоритмы пауз, имитация поведения человека и автоматическое нажатие "Мне есть 18 лет".
-   **🔍 Аудит:** Функция диагностики, которая сравнивает вашу локальную папку с сайтом и показывает, какие работы отличаются (были удалены или не скачаны).

## 🚀 Установка и запуск (для пользователей)

1.  Перейдите в раздел **[Releases](../../releases)**.
2.  Скачайте последнюю версию MyFicbook.exe.
3.  Запустите `MyFicbook.exe`.\
    *(Браузер Google Chrome должен быть установлен на компьютере)*.
4.  Для начала и создания папки с сборниками ваших фанфиков нажмите "Скачать (Синхронизация)". Появится окно с инструкцией, жмите "Понятно". Программа откроет браузер с входом в аккаунт. Ввойдите с помощью ваших логина и пароля. Вернитесь в окно программы и нажмите "Ок". После чего начнётся сбор ваших сохранённых фанфиков из Избранного. Для безопасного завершения работы жмите "Остановить".

## 🛠 Запуск из исходного кода (для разработчиков)

Если вы хотите запустить скрипт через Python или собрать свой EXE.

### Требования

-   Python 3.8+
-   Google Chrome

### Установка

1.  Клонируйте репозиторий:

``` bash
git clone https://github.com/ВАШ_НИК/Ficbook-Librarian.git
cd Ficbook-Librarian
```

2.  Создайте виртуальное окружение и установите зависимости:

``` bash
python -m venv venv
# Windows:
.\venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

pip install -r requirements.txt
```

3.  Запустите:

``` bash
python gui.py
```

### Сборка в .EXE

Используется `pyinstaller`. Чтобы иконка встроилась корректно, используйте команду:

``` bash
pyinstaller --noconsole --onefile --icon="feather.ico" --add-data "feather.ico;." --name "MyFicbook" gui.py
```

## 🧩 Как это работает?

Программа использует `undetected-chromedriver` для управления реальным браузером Chrome. Она заходит в ваш аккаунт, открывает каждый сборник и последовательно "читает" текст глав, сохраняя его в файлы.

-   **Файл `history.txt`** --- хранит базу ссылок уже скачанных работ.
-   **Файл `!ВСЯ_БИБЛИОТЕКА.txt`** --- глобальное оглавление всех ваших
    сборников.

## ⚠️ Важно

-   Программа работает через ваш браузер. Не закрывайте открывшееся окно Chrome в процессе работы.
-   Для работы сайта в РФ необходим VPN.
-   Используйте кнопку **⛔ СТОП** в интерфейсе для безопасного  прерывания процесса (чтобы сохранить текущую книгу и обновить Оглавление).

## 📄 Лицензия

MIT License. Используйте свободно, но с умом. Автоматизация действий на сайтах может нарушать их правила. Используйте на свой страх и риск.
